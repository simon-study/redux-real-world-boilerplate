// import { fromJS } from 'immutable';
// import { selectArticleDetailContainerDomain } from '../selectors';

describe('selectArticleDetailContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
